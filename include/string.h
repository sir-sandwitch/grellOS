extern int strlen(char str[]);

extern char* strcat(char* str1, char* str2);

extern char* strncat(char* str1, char* str2, int n);

extern char* strcpy(char s1[], char s2[]);

extern void reverse(char* str);

extern char* ltoa(long num, char* str, int base);